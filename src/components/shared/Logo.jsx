export default function Logo({ className = '' }) {
  return (
    <img
      src="/bespaarcheck/Logo bespaarcheck.png"
      alt="BespaarCheck"
      className={`h-10 w-auto ${className}`}
    />
  )
}
